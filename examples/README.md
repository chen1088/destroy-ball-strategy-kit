# Example Strategies

These files are public reference strategies for the destroy-ball strategy kit.

You can run any of them with:

```bash
npm run run -- examples/momentum-reader.js
```

Suggested workflow:

1. Run one of the examples.
2. Copy the parts you like into `strategy.js`.
3. Change the logic.
4. Run `npm test` again.